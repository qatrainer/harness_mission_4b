pipeline:
  name: Simple Pipeline
  identifier: Simple_Pipeline
  projectIdentifier: default_project
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: account.account_level_github_connector
        repoName: harness_mission_4b
        build: <+input>
  stages:
    - stage:
        name: Run script
        identifier: Run_script
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: true
          buildIntelligence:
            enabled: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: account.paul_build_farm_connector
              namespace: harness-delegate-ng
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          execution:
            steps:
              - step:
                  type: Run
                  name: Run script
                  identifier: Run_script
                  spec:
                    connectorRef: account.account_level_dockerhub_connector
                    image: bash
                    shell: Sh
                    command: sh run.sh
